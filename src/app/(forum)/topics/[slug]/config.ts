// This config ensures that topics are generated dynamically
// and are not statically cached at build time
export const dynamic = 'force-dynamic';
export const revalidate = 0; // Revalidate the data on every request 